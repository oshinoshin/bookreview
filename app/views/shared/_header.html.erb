<nav class="navbar navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand">BOOKREVIEW</a>

<%# ユーザーがログインしていたら表示を変える %>
      <header class="header">
        <div class="header__bar row">
          <% if user_signed_in? %>
            <div class="user_nav grid-6">
              <span><%= current_user.name %>
                <ul class="user__info">
                  <li>
                    <%= link_to "マイページ", user_path(current_user.id),
                    class: "btn btn-default" %>
                    <%= link_to "ログアウト", destroy_user_session_path, method: :delete  ,class: "btn btn-default"%>
                  </li>
                </ul>
              </span>
              <%= link_to "投稿する", new_book_path, class: "post" %>
            </div>
          <% else %>
            <div class="grid-6">
              <br>
                <%= link_to "ログイン", new_user_session_path, class: "post", class: "btn btn-default" %>
                <%= link_to "新規登録",  new_user_registration_path, class: "post", class: "btn btn-default" %>
                <br/>
            </div>
          <% end %>
      </header>
<%# ユーザーがログインしていたら表示を変える %>

<%# Searchボタン %>
<%= form_with(url: search_books_path, local: true, method: :get, class: "search-form") do |form| %>
  <%= form.text_field :keyword, placeholder: "書籍を検索する", class: "search-input" %>
  <%= form.submit "検索", class: "search-btn" %>
<% end %>
<%# Searchボタン %>

  </div>
</nav>

